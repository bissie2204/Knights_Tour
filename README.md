# Knights_Tour
**I6. Knight's Tour Problem Solver**  **Objective:** Solve the Knight's Tour using Warnsdorff's heuristic. The knight tour is to move a knight in a 8x8 chessboard such that the knight covers all squares in the chessboard only once.

## Requirements

- Python 3.8+ (tested on 3.9+)
- matplotlib
- numpy

Install requirements with pip:

```bash
python -m pip install matplotlib numpy